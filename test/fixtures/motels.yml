
<% 10.times do |n| %>
item_<%= n %>:
  title: <%= n % 2 == 0 ? 'Banana House' : 'Harmony resort' %>
  rating: <%= n % 3 == 0 ? 5 : 4 %> 
  price: <%= n % 3 == 0 ? 111 : 222 %> 
  description: <%= n % 2 == 0 ? 'Samui Harmony Resort on Lamai Beach offers wooden  ' : 'built bungalows either by the seaside or canal and blend with Koh Samui nature and tropical' %>
<% end %>
